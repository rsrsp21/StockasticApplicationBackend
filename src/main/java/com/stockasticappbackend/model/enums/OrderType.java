package com.stockasticappbackend.model.enums;

/**
 * Enum representing the type of order.
 */
public enum OrderType {
    /** Buy order to purchase stocks. */
    BUY,
    /** Sell order to sell owned stocks. */
    SELL
}
